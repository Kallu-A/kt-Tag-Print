/** main launch the app*/
fun main() {
    println(error("Issue on the runtime"))
    println(warning("Be careful to that"))
    println(info("Information for the user"))
    println(data("important data to show"))
    println(deadly("end of the program because of an error"))
    println(comment("Comment for the dev"))

    print(error())
    print(deadly())
    print(warning())
    println(data("You can combine the markup"))

}